# Instructions append

# Return value

If the item is present, return a positive integer index.

Otherwise, return the negative of the index the item would be assigned if it were inserted now.

For example, if we are looking for the number 50 in the sorted list `[20, 40, 60, 80]` we should return -3.
