-- Define the weekschedule type with/and permitted values first, second, third, fourth, fifth, teenth, and last
-- Define the weekday type with/and permitted values Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, and Sunday

global function day(weekschedule w, weekday dow, integer m, y)
    -- return a meetup date in the format "YYYY-MM-DD"
    crsh("please implement day")
end function

